{
  "name": "opencode-vision-bridge",
  "version": "0.2.0",
  "description": "Give text-only models (e.g. DeepSeek) image understanding via a vision model — host-agnostic core with OpenCode + pi adapters",
  "type": "module",
  "main": "src/index.ts",
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test \"tests/*.test.mjs\""
  },
  "license": "MIT",
  "dependencies": {
    "@opencode-ai/plugin": "^1.18.11"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.83.0",
    "@sinclair/typebox": "^0.34.13",
    "esbuild": "^0.25.0"
  }
}
